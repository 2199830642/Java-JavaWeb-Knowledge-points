package chen.study.Day13Stream流_LambdaAPI.Stream流式思想;


/*
    Stream流的常见方法filter(过滤)，map(映射:a元素->ab),skip(跳过)，cout(计数)

    执行这些操作的时候，集合元素并没有产生影响，而只是对函数模型进行操作

    Stream流是一个来自数据源的元素队列
        元素使特定类型的对象，形成一个队列，java中的Stream并不会存储元素，而是按需计算
        "数据源"流的来源，可以是集合，数组等
 */
public class Demo03流式思想概述 {
    public static void main(String[] args) {

    }
}
