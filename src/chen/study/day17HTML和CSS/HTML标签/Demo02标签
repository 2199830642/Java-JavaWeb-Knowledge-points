标签学习：
        1.文件标签：构成html最基本的标签
            html:html文档的根标签
            head:头标签，用于指定html文档的一些属性，引入外部的资源
            title:定义标题
            body:体标签
            <!DOCTYPE html>:html5文档中定义该文档是html文档

        2.文本标签：和文本有关的标签
            注释：<!--注释内容-->
            <h1> to <h6>:标题标签；<h1></h1>
            <p>:段落标签<p></p>
            <br/>:换行标签
            <hr>:显示一条水平线
                    属性：color颜色
                          width长度
                          size高度
                          align:对齐方式
                                center 居中 left左对齐 right右对齐

            <b>:字体加粗
            <i>:字体斜体
            <font>:字体标签,已过时
                    属性：color 颜色
                         size 大小
                         face 字体
            <center>:相对于父元素文本居中，已被淘汰

            属性定义：
                color：
                    1.英文单词：green，red，blue
                    2.rgb(值1，值2，值3) 值的范围0~255  红绿蓝三原色的成分
                    3.#值1值2值3：值的范围00~FF之间

                width：
                    1.数值：width='20',数值的单位默认为px（像素）
                    2.数值%：占比相对于父元素的一个比例

        3.图片标签：
            img:展示图片
                属性：
                    src：用来指定照片路径
                    相对路径
                        以.开头的路径
                            ./代表当前目录  如： ./image/1.jpg
                            ../代表上一级目录

        4.列表标签：
            有序列表：
                ol:
                    属性：type，设置顺序方式
                         start，从第几个开始
                li:
            无序列表
                ul:
                    属性：type disc实心圆点显示，square实心方框，circle空心圆圈
                li:
        5.链接标签：
            a:定义一个超链接
                属性：
                    href:指定访问资源的URL（统一资源定位符）
                    target: 指定打开资源的方式
                            _self:在本页面打开超链接，默认是这个
                            _blank:另打开一个空白页面，显示超链接内容

        6.div和span:
                div：每一个div占满一整行，块级标签
                span：文本信息在一行展示，行内标签，内联标签

        7.语义化标签:html5中为了提高程序的可读性，提供了一些标签，没有任何样式，需要结合css
                1.<header>:
                2.<footer>:

        8.表格标签:
                1.table:定义表格
                    属性：
                        width:宽度
                        border:边框
                        cellpadding:单元格与边框的距离，指定为0
                        cellspacing:定义单元格之间的距离，如果指定为0，则单元格之间的线会合为一条
                        bgcolor:背景色
                        align:对齐方式，如果是center那么表格就会移到页面中间

                2.tr:定义行
                    属性：
                        bgcolor：背景色
                        align：对齐方式

                3.th:定义表头单元格
                4.td:定义单元格
                    属性：
                        colspan：合并列
                        rowspan：合并行

                5.caption:定义表格标题
                6.thead:表示表格头部分
                7.tbody:表示表格体部分
                8.tfoot:表示表格脚部分